# Progress - 2026-05-08

## Session: Naver Map mobile route UX reference

- Agent/model: Hermes / gpt-5.5
- Route selected: meeting
- Changed files:
  - `docs/design/naver-map-mobile-route-ux-2026-05-08.md`
- Summary:
  - 사용자가 제공한 네이버지도 모바일 검색/장소상세/경로설정/경로결과 스크린샷을 기준으로 MidWayDer 모바일 UX 기준 문서를 작성했다.
  - 구현 전 기준선으로 `idle`, `placeSelected`, `routeEditing`, `results` 상태 모델과 컴포넌트별 적용 계약을 정리했다.
- Verification:
  - 문서 파일 작성 완료.
  - 코드 변경 없음. 테스트 미실행.
- Residual risks:
  - 실제 구현은 아직 진행하지 않았다.
  - 다음 단계에서 `SearchOverlay`, `MobileHomeShell`, `ResultSheet`, `PlaceDetail`의 활성 컴포넌트 트리를 확인한 뒤 문서 기준으로 재구성해야 한다.

## Session: Naver Map mobile route UX implementation

- Agent/model: Hermes / gpt-5.5
- Route selected: build
- Changed files:
  - `src/components/search/SearchOverlay.tsx`
  - `src/components/home/MobileHomeShell.tsx`
  - `docs/progress/2026-05-08.md`
- Summary:
  - 모바일 검색 오버레이를 네이버지도식 경로 설정 구조로 재정리했다.
  - 상단 floating search header, 빠른 장소 칩, 2줄 출발/도착 route card, 48px 교통수단 segmented tab, 낮은 카테고리/최근검색 row, safe-area 하단 CTA를 적용했다.
  - 모바일 키보드 검색/Enter가 하단 CTA와 같은 검색 경로로 실행되도록 유지했다.
  - 모바일 결과 시트 접기/펼치기 E2E 안정성을 위해 결과 시트 height transition을 제거했다.
- Verification:
  - `npm run type-check` 통과.
  - `npm run lint` 통과.
  - `npm run test:e2e:mobile:ui` 통과: 11 passed.
  - Playwright iPhone 375x812 로컬 화면 확인: route card 88px, transport tabs 48px, footer bottom 812px, CTA 48px.
  - Screenshot: `/tmp/midwayder-naver-mobile-ux-implemented.png`.
- Deployment:
  - Commit `600a8ec` pushed to `origin/main`.
  - Railway production deploy completed for the commit.
- Residual risks:
  - 저장소에 이번 작업과 무관한 기존 변경이 다수 남아 있어 이후 작업에서도 staged 범위 확인이 필요하다.

## Session: Mobile route editor visibility hotfix

- Agent/model: Hermes / gpt-5.5
- Route selected: build
- Changed files:
  - `src/components/search/SearchOverlay.tsx`
  - `tests/e2e/mobile-ui.spec.ts`
  - `docs/progress/2026-05-08.md`
- Summary:
  - 모바일 검색 오버레이 첫 화면에서 2줄 출발지/도착지 입력 카드와 교통수단 탭이 계속 노출되지 않도록 수정했다.
  - 출발지/도착지가 이미 있는 경우에는 기존처럼 바로 편집 카드가 열리고, 빈 상태에서는 `출발지 · 도착지 입력` compact 설정 행만 노출된다.
  - 설정 행이나 상단 검색 pill을 누르면 route editor가 열리고 기존 키보드 검색/CTA 경로는 유지된다.
- Verification:
  - `npm run type-check` 통과.
  - `npm run lint` 통과.
  - `npm run test:e2e:mobile:ui` 통과: 11 passed.
  - Playwright iPhone 375x812 로컬 확인: initial `mobile-route-input-card` hidden, `mobile-route-edit-trigger` visible, trigger click 후 route card visible.
  - Screenshot: `/tmp/midwayder-route-input-hidden-mobile.png`, `/tmp/midwayder-route-input-expanded-mobile.png`.
- Residual risks:
  - Railway 배포 전 상태. 배포 시 exact commit 기준으로 라이브 확인 필요.

## Session: Native map route state visibility pass

- Agent/model: Hermes / gpt-5.5
- Route selected: build
- Changed files:
  - `docs/design/naver-map-mobile-route-ux-2026-05-08.md`
  - `src/components/home/MobileSearchEntry.tsx`
  - `src/components/home/MobileHomeShell.tsx`
  - `src/components/search/SearchOverlay.tsx`
  - `tests/e2e/mobile-ui.spec.ts`
  - `docs/progress/2026-05-08.md`
- Summary:
  - 기준 문서에 `idle`, `routeEditing`, `results`, `placeSelected` 상태별 노출 계약과 완료 기준을 확정했다.
  - 모바일 기본 화면에서 하단 `경로 입력` CTA 카드를 제거하고 compact search pill 중심으로 정리했다.
  - 검색 오버레이는 처음에는 compact 설정 행만 보이고, 명시적으로 경로 설정에 들어갔을 때만 2줄 출발지/도착지 route card, 교통수단 탭, 카테고리 rail, safe-area CTA가 보이도록 조정했다.
  - 결과 화면은 compact summary pill과 하단 result sheet 중심으로 확인했고, full route editor는 숨김 상태를 유지했다.
- Verification:
  - `npm run type-check` 통과.
  - `npm run lint` 통과.
  - `npm run test:e2e:mobile:ui` 통과: 11 passed.
  - `scripts/harness-check.sh --diff HEAD` 통과: 검사할 변경 파일 없음.
  - Playwright iPhone 375x812 로컬 확인: idle search pill visible, idle route card hidden, heavy CTA hidden, routeEditing card/transport visible, CTA disabled when 조건 부족.
  - Playwright iPhone 375x812 결과 상태 확인: summary pill `구의역 2호선 → 명지대` visible, result sheet visible, full route card hidden, placeSelected 액션 visible.
  - Screenshots: `test-results/manual-mobile-idle-375.png`, `test-results/manual-mobile-route-editing-375.png`, `test-results/manual-mobile-results-375.png`, `test-results/manual-mobile-place-selected-375.png`.
- Deployment:
  - Commit `f3a86ae` pushed to `origin/main`.
  - GitHub-triggered Railway deployment was skipped because the CI check suite failed.
  - Manual Railway deployment `61661720-a544-4ab1-9b28-5c01e5ea1be3` completed with `SUCCESS` for the exact commit.
- Live verification:
  - `https://midwayder.up.railway.app` checked at iPhone 375x812 viewport.
  - Idle state: compact search pill visible, route card hidden, heavy `경로 입력` CTA hidden.
  - Results state: summary pill `구의역 2호선 → 명지대` visible, result sheet visible, full route card hidden.
  - Screenshots: `/tmp/midwayder-live-mobile-idle-375.png`, `/tmp/midwayder-live-mobile-results-375.png`.
- Residual risks:
  - 저장소에 이번 변경과 무관한 기존 dirty/untracked 파일이 다수 있어 이후 작업에서도 touched file 범위 확인이 필요하다.
