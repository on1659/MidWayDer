# Progress - 2026-05-07

## 00:18 세션 - mobile-search-loading-simplify

### 세션 정보
- 세션명: `mobile-search-loading-simplify`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 문제
- 모바일 검색 중 `경로 분석 중 / 장소 탐색 중 / 비용 계산 중` 대형 단계 카드와 skeleton 카드가 계속 노출됨.
- 실제 사용자 화면에서 검색 중 UI가 무겁고 이전 개선과 체감 차이가 없음.

### 작업 내역
- [UX] `ResultList` 로딩 UI를 큰 단계형 카드 + skeleton 리스트에서 작은 상태 카드로 축소.
- [UX] 모바일에서는 검색 중 `mobile-result-sheet` 바텀시트를 숨기고 상단 검색 진입점에 `찾는 중...`만 표시.
- [UX] `SearchOverlay` 검색 중 문구도 단계별 메시지 대신 `찾는 중...`으로 단순화.
- [Test] `ResultList` 로딩 테스트를 큰 단계 카드 미표시 검증으로 변경.
- [E2E] 모바일 검색 지연 상황에서 대형 단계 카드/skeleton 바텀시트가 보이지 않는 회귀 테스트 추가.

### 변경 파일
- `src/app/page.tsx`
- `src/components/search/ResultList.tsx`
- `src/components/search/SearchOverlay.tsx`
- `src/components/search/__tests__/ResultList.test.tsx`
- `tests/e2e/mobile-ui.spec.ts`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npx vitest run src/components/search/__tests__/ResultList.test.tsx src/store/__tests__/search-store.test.ts` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 실제 API가 느린 경우 지도 위 상단 버튼에만 진행 상태가 노출됨.
- 배포 후 모바일 실기기에서 검색 중 화면 재확인 필요.

## 00:28 세션 - remove-legacy-loading-stage-code

### 세션 정보
- 세션명: `remove-legacy-loading-stage-code`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 원인 분석
- 대형 검색 로딩 카드 UI는 제거했지만, `LOADING_STAGES`와 `useLoadingStages` 레거시 코드가 남아 있었음.
- 배포 산출물에서 `경로 분석 중 / 장소 탐색 중 / 비용 계산 중` 문자열이 계속 발견되어 실제 체감상 바뀌지 않은 것처럼 보일 수 있었음.
- 프로덕션 응답은 `x-nextjs-cache: HIT`, `s-maxage=31536000`으로 캐시가 강하게 잡혀 있어 배포/서비스워커 캐시까지 겹치면 변경 체감이 늦을 수 있음.

### 작업 내역
- [Cleanup] `LOADING_STAGES` 레거시 상수 제거.
- [Cleanup] `useLoadingStages` 훅과 전용 테스트 삭제.
- [Verify] 앱 소스에서 대형 단계 문구가 테스트의 부정 검증 외에는 남지 않도록 확인.

### 변경 파일
- `src/components/search/result-list/utils.ts`
- `src/components/search/result-list/hooks/useLoadingStages.ts` 삭제
- `src/components/search/result-list/hooks/__tests__/useLoadingStages.test.ts` 삭제
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npx vitest run src/components/search/__tests__/ResultList.test.tsx src/store/__tests__/search-store.test.ts` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 배포 URL에서 이전 chunk가 남아 있으면 Railway/Next 캐시 또는 브라우저 서비스워커 캐시를 무효화해야 함.

## 00:40 세션 - frontend-reset-planning

### 세션 정보
- 세션명: `frontend-reset-planning`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `meeting`

### 확인한 흔적
- `docs/design/frontend-reset-meeting-2026-05-05.md`: 기존 UI/UX 마이그레이션 중단, 홈 프론트엔드 신규 설계 결정.
- `docs/design/component-migration-checklist.md`: `src/app/page.tsx`를 새 홈 shell 기준으로 두고 `ResultList`, `SearchOverlay`, `PlaceDetail` 순서로 재설계.
- `docs/design/2026-modernization-proposal-ui.md`: glass stack, expressive typography, dynamic pill, result card mockup 기준.
- `docs/design/2026-modernization-proposal-mobile.md`: 모바일 sheet drag, haptic, spring motion, live activity bar.
- `docs/design/mockups/`: `mobile.html`, `desktop.html`, `tokens.html` 실제 목업 존재 확인.

### 작업 내역
- [Plan] 전면 재설계 실행 계획을 `docs/plans/2026-05-07-frontend-reset-ux-ui.md`에 작성.
- [Decision] 이번 방향은 partial polish가 아니라 `idle/searching/results/detail` 4상태 shell 재구축으로 확정.

### 변경 파일
- `docs/plans/2026-05-07-frontend-reset-ux-ui.md`
- `docs/progress/2026-05-07.md`

### 검증
- 문서 작업이라 코드 검증 미실행.
- 다음 build 진입 시 `npm run type-check`, `npm run lint`, `npm run test:e2e:mobile:ui`, `scripts/harness-check.sh --staged` 실행 필요.

### 리스크 / 후속
- 작업트리에 기존 미커밋 변경이 많으므로, build 단계는 파일 단위로 엄격히 stage해야 함.
- 실제 체감 변경을 위해 다음 단계에서 `src/app/page.tsx`의 새 shell boundary부터 구현해야 함.

## 01:15 세션 - mobile-home-shell-reset-pass

### 세션 정보
- 세션명: `mobile-home-shell-reset-pass`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 작업 내역
- [Shell] `HomeShell` 경계를 추가해 지도/데스크톱/모바일/오버레이 렌더링을 새 홈 shell 구조로 분리.
- [Mobile] `MobileHomeShell`, `MobileSearchEntry`, `MobileCategoryRail` 신규 작성.
- [Mobile] 검색 전 화면을 glass search entry + 카테고리 rail + 하단 idle prompt로 재구성.
- [Mobile] 검색 중에는 live status pill만 표시하고 결과 바텀시트/skeleton은 숨김.
- [Mobile] 결과 바텀시트를 신규 카드 레이아웃으로 교체: 추천 수, 저장/조건수정, progress chips, BEST 카드, 추가/거리/점수 stat pods.
- [Cleanup] 기존 `page.tsx` 내부 모바일 검색 버튼/결과 바텀시트 JSX를 새 컴포넌트로 대체.
- [Test] 모바일 idle shell, category rail, 새 결과 액션 문구 검증을 E2E에 반영.

### 변경 파일
- `src/app/page.tsx`
- `src/components/home/HomeShell.tsx`
- `src/components/home/MobileHomeShell.tsx`
- `src/components/home/MobileSearchEntry.tsx`
- `src/components/home/MobileCategoryRail.tsx`
- `src/components/home/types.ts`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 이번 1차는 모바일 shell 우선임. `SearchOverlay`, `PlaceDetail`, 데스크톱 shell은 다음 pass에서 별도 리디자인 필요.
- E2E 산출물 때문에 lint가 `playwright-report/`를 읽는 문제가 있어 검증 전 `test-results`, `playwright-report` 정리가 필요함.

## 01:35 세션 - contrast-readability-hotfix

### 세션 정보
- 세션명: `contrast-readability-hotfix`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 문제
- 현재 화면에서 좌측 패널/상세 카드의 글자색과 배경색 대비가 낮아 텍스트가 안 보임.
- 다크 테마 토큰과 흰색 유틸리티 배경이 섞여 장소 카드가 `흰 배경 + 밝은 글자`처럼 보일 수 있었음.
- 카카오 지도 기본 색상이 옅어서 도로/지도 배경 대비가 약함.

### 작업 내역
- [Theme] 다크 테마의 `text-primary/secondary/muted/strong` 대비를 상향.
- [Theme] 명시적 `theme-light` 클래스를 초기화/토글에 추가해 OS dark fallback이 토큰만 바꾸는 문제를 차단.
- [Theme] OS dark fallback은 app chrome 토큰 변경 대신 light color-scheme만 유지하도록 축소.
- [Map] 카카오맵 컨테이너에 `map-contrast-canvas` 필터를 적용해 도로/지도 색 대비와 채도를 올림.
- [Place] `MapClickSheet`의 하드코딩 `bg-white`를 semantic surface/background로 교체.

### 변경 파일
- `src/app/theme.css`
- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/hooks/useTheme.ts`
- `src/components/map/MapContainer.tsx`
- `src/components/place/MapClickSheet.tsx`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 지도 타일 자체 디자인은 카카오맵 제공 이미지라 CSS filter 수준의 보정임. 더 강한 대비가 필요하면 지도 provider/style 전환 검토 필요.

## 01:55 세션 - mobile-search-overlay-opacity-fix

### 세션 정보
- 세션명: `mobile-search-overlay-opacity-fix`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 문제
- iPhone Safari 화면에서 검색 오버레이가 열려 있는데 지도/홈 shell/카테고리/하단 prompt가 뒤에서 그대로 비침.
- 오버레이 안의 입력 UI와 홈 화면 UI가 겹쳐 모바일에서 화면이 망가져 보임.

### 원인
- `SearchOverlay`가 `--bg-primary`, `--bg-hover`, `--border-subtle` 같은 현재 테마에 없는 legacy CSS var를 사용함.
- 그래서 최상위 fixed dialog 배경이 `transparent`가 되고, underlying map/home shell이 그대로 노출됨.
- 카카오맵 컨트롤이 높은 z-index로 남을 수 있어 오버레이 격리가 약함.

### 작업 내역
- [Fix] `SearchOverlay` 최상위 dialog 배경을 존재하는 `var(--bg-app)`으로 교체.
- [Fix] legacy `--bg-hover`, `--border-subtle` 참조를 `--bg-surface-muted`, `--border-soft`로 교체.
- [Fix] 오버레이 본문을 내부 스크롤 컨테이너로 감싸 fixed panel 내부에서만 스크롤되게 조정.
- [Fix] 오버레이 z-index를 map control보다 확실히 위로 상향.
- [Test] 모바일 오버레이 E2E에 불투명 배경/상단 hit target 검증 추가.

### 변경 파일
- `src/components/search/SearchOverlay.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed
- 로컬 iPhone 14 Pro Playwright 캡처에서 dialog 배경 `rgb(246, 247, 249)`, z-index `2147483000` 확인.

### 리스크 / 후속
- 이번 수정은 검색 오버레이 붕괴 hotfix임. SearchOverlay 자체 UX는 아직 legacy 구조라 다음 pass에서 새 모바일 입력 화면으로 재설계 필요.

## 02:58 세션 - mobile-results-readability-pass

### 세션 정보
- 세션명: `mobile-results-readability-pass`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 문제
- iPhone Safari 결과 화면에서 바텀시트가 진한 남색 계열이라 지도/카드/텍스트가 한 덩어리처럼 보임.
- 장소명, 주소, 추가 시간, 거리, 점수 같은 핵심 정보가 작고 대비가 약해 읽기 어려움.
- 카테고리 chip도 어두운 반투명 배경이라 지도 위에서 분리감이 약함.

### 원인
- `MobileHomeShell` 결과 시트가 테마 변수 기반 다크 surface를 그대로 사용함.
- 결과 카드와 stat pod가 같은 계열의 어두운 배경을 반복해 정보 계층이 약해짐.
- 제목/주소/stat 값 글자 크기와 weight가 실기기 기준으로 부족함.

### 작업 내역
- [Fix] 모바일 결과 시트를 고대비 light surface(`#f8fafc`)로 고정해 지도/결과 영역 분리.
- [Fix] 결과 카드는 흰색 카드 + 진한 slate 텍스트로 변경.
- [Fix] 장소명 18px, stat 값 18px, CTA 16px로 키우고 weight를 올림.
- [Fix] stat pod 배경/테두리 대비 강화, 선택 카드/Best badge 색상 정리.
- [Fix] 모바일 카테고리 rail 미선택 chip을 흰색 계열로 변경해 지도 위 가독성 강화.
- [Test] 모바일 결과 시트가 다크 모드에서도 고대비 light 결과 패턴을 유지하는 회귀 테스트 추가.
- [Test] splash 중복 DOM으로 strict locator가 흔들리지 않도록 첫 splash 기준으로 대기 보정.

### 변경 파일
- `src/components/home/MobileHomeShell.tsx`
- `src/components/home/MobileCategoryRail.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 이번 수정은 결과 정보 가독성 hotfix임. 전체 모바일 결과 UX는 다음 pass에서 카드 수/시트 높이/지도 visible area까지 다시 정리 필요.

## 07:52 세션 - mobile-results-map-first-sheet

### 세션 정보
- 세션명: `mobile-results-map-first-sheet`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 작업 내역
- [Mobile] 결과 바텀시트 최대 높이를 `66dvh`에서 `58dvh`로 낮춰 지도 가시 영역을 우선 확보.
- [Mobile] 결과 시트 좌우 여백을 넓혀 iPhone 화면에서 edge clipping을 줄임.
- [Test] 모바일 결과 시트가 화면 60% 이하를 차지하고, 상단 검색 진입점과 충분히 분리되는 회귀 검증 추가.
- [Test] 결과 리스트는 축소된 시트 내부에서 스크롤해 하위 결과까지 확인하도록 보정.

### 변경 파일
- `src/components/home/MobileHomeShell.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed
- `scripts/harness-check.sh --diff HEAD` 통과: 검사할 변경 파일 없음

### 리스크 / 후속
- 실제 iPhone Safari에서 시트 높이 체감 확인 필요.
- 다음 모바일 우선 작업은 `SearchOverlay` 자체 UX 재설계 또는 결과 시트 drag/collapse 상태 도입이 적절함.

## 09:05 세션 - mobile-search-overlay-ux-pass

### 세션 정보
- 세션명: `mobile-search-overlay-ux-pass`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 작업 내역
- [Mobile] 검색 오버레이 헤더를 낮추고 설명을 추가해 입력 목적을 명확히 함.
- [Mobile] 출발/도착 입력을 하나의 카드로 묶고 중복 현재 위치 CTA를 제거.
- [Mobile] 카테고리 선택도 별도 카드로 분리해 화면 구조를 단순화.
- [Mobile] 검색 CTA를 하단 footer로 고정해 스크롤 중에도 바로 실행 가능하게 함.
- [Fix] 공통 `keyboard-aware`/slide-up 조합이 footer를 전체 화면 hitbox로 키우는 문제를 제거해 상단 뒤로가기/테마 버튼 터치를 보존.
- [Test] 모바일 오버레이 카드/하단 CTA 고정 상태 회귀 검증 추가.

### 변경 파일
- `src/components/search/SearchOverlay.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 9 passed

### 리스크 / 후속
- 실제 iPhone Safari에서 키보드가 올라온 상태의 sticky CTA 위치 체감 확인 필요.

## 13:16 세션 - mobile-result-sheet-collapse

### 세션 정보
- 세션명: `mobile-result-sheet-collapse`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 작업 내역
- [Mobile] 결과 시트 핸들을 버튼화해 접기/펼치기를 지원.
- [Mobile] 시트 접힘 상태를 `34dvh`, 펼침 상태를 `58dvh`로 분리해 지도 가시 영역을 즉시 조절 가능하게 함.
- [Mobile] 핸들 터치 드래그 방향으로 접힘/펼침이 바뀌도록 보정.
- [Test] 모바일 결과 시트 접기/펼치기 상태와 하단 고정 geometry 회귀 검증 추가.

### 변경 파일
- `src/components/home/MobileHomeShell.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 10 passed

### 리스크 / 후속
- 실제 iPhone Safari에서 손가락 드래그 감도 체감 확인 필요.

## 16:09 세션 - mobile-result-compact-controls

### 세션 정보
- 세션명: `mobile-result-compact-controls`
- 에이전트: `Hermes`
- 모델: `gpt-5.5`
- 하네스 route: `build`

### 작업 내역
- [Mobile] 결과 시트 상단 `저장`/`조건 수정` 텍스트 버튼을 작은 아이콘 버튼으로 압축.
- [Mobile] 구간 표시 pill 높이와 배경 강조를 줄여 결과 카드가 더 빨리 보이게 함.
- [Mobile] 결과 카드의 큰 `지도에서 보기` CTA를 제거하고 카드 우측 작은 지도 아이콘으로 대체.
- [Mobile] `추가/거리/점수` 3분할 박스를 한 줄 메타 chip으로 압축하고 순위 원 크기도 축소.

### 변경 파일
- `src/components/home/MobileHomeShell.tsx`
- `tests/e2e/mobile-ui.spec.ts`
- `docs/progress/2026-05-07.md`

### 검증
- `npm run type-check` 통과
- `npm run lint` 통과
- `npm run test:e2e:mobile:ui` 통과: 10 passed

### 리스크 / 후속
- 실제 iPhone Safari에서 카드 탭 영역 체감 확인 필요.
