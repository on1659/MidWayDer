# /build — 이더(Ether) 오케스트레이션 빌드

$ARGUMENTS 요청을 이더(Ether) 하네스 파이프라인으로 실행해줘.

`.claude/skills/harness/SKILL.md`의 지시를 따라:

1. 트리아지 판정 (SIMPLE/STANDARD/COMPLEX)
2. STANDARD/COMPLEX면 Scout 정찰 → 지시서 작성
3. Coder 구현 → Reviewer 리뷰 → (COMPLEX면 QA 검증)
4. 최종 보고

불변조건을 반드시 지켜라. main = 실서버.
