# AutoMeeting — 2026-02-28 14:59

## 현황 (v0.5.1)

ResultList.tsx는 2,400+ 라인의 복합 컴포넌트로, 지금까지 구현된 주요 기능:
- 빠른 필터 칩, 컴팩트 카드 토글, 스와이프 액션
- 베스트픽 배너, 점수 분해 토글, 경로 구간 차트
- 실시간 ETA, 영업시간 타임라인, 출발 시각 입력
- 방문 기록, 즐겨찾기, 메모, 핀 고정
- 모바일 하단 고정 요약 바

---

## UX 분석 — 개선점 점수화

| # | 항목 | 점수 | 이유 |
|---|------|------|------|
| 1 | ⏱ 출발 시각 빠른 설정 (+30분/+1h/+2h) | **15점** | 시간 input 직접 조작이 모바일에서 불편. 빠른 프리셋이 98% 케이스 커버 |
| 2 | 💬 피드백 섹션 축소 | **11점** | 큰 흰 카드 + 두 개 큰 버튼이 스크롤 공간 낭비. 실 클릭률 낮음 |
| 3 | 🔍 카드 확대 모달 (PlaceDetail) | **14점** | 주소/전화/메모가 오른쪽 아이콘 세로 스택에 숨겨 있어 발견성 낮음 |
| 4 | 🎯 근접도 필터 칩 추가 | **9점** | proxScore > 70 필터로 경로 바로 옆 장소만 노출 |
| 5 | 📐 이탈 거리 필터 (+1km/+2km) | **8점** | 시간 필터만 있고 거리 필터 없음. 자전거/도보 사용자 고려 |
| 6 | 🔊 결과 TTS 읽어주기 | **6점** | "베스트 픽은 XX입니다. +N분 추가됩니다" 운전 중 hands-free |
| 7 | 🏷️ 뱃지 그룹핑 | **7점** | 전체 카드 뱃지 8+개가 난잡. 1차(km/분/점수) + 토글 2차(위치/인기도) |
| 8 | 📊 내보내기 버튼 아이콘 툴팁 | **4점** | 📋 아이콘만 있어 기능 불명확. label 추가 필요 |

---

## 우선순위 결정 → 구현 상위 2개

### 1위: ⏱ 출발 시각 빠른 설정 (15점)
- 이유: MEMORY.md 1순위로 명시된 미구현 기능
- 모바일 `<input type="time">` UX가 OS마다 달라 불편

### 2위: 💬 피드백 섹션 축소 (11점)
- 이유: 큰 흰 카드가 리스트 하단을 40px+ 점유
- 실제 피드백 클릭률은 낮고, 결과 저장 CTA 아래 묻힘

---

## 구현 내역

### ① 출발 시각 빠른 설정 버튼 — `ResultList.tsx:1207`

```tsx
{/* ⏱ 빠른 출발 시각 설정 버튼 (+30분/+1시간/+2시간) */}
<div className="flex items-center gap-1.5">
  {([30, 60, 120] as const).map((mins) => (
    <button
      key={mins}
      onClick={() => {
        const d = new Date(Date.now() + mins * 60000);
        setDepartureTime(`${String(d.getHours()).padStart(2, '0')}:${String(d.getMinutes()).padStart(2, '0')}`);
      }}
      className="flex-1 px-2 py-1 rounded-lg text-[11px] font-semibold transition-all active:scale-95"
      style={{ background: 'var(--blue-100)', color: 'var(--blue-700)', border: '1px solid var(--blue-200)' }}
    >
      +{mins < 60 ? `${mins}분` : `${mins / 60}시간`}
    </button>
  ))}
</div>
```

**효과**: 출발 시각 input 바로 아래 "+30분", "+1시간", "+2시간" 3개 칩. 클릭 즉시 ETA 재계산.

### ② 피드백 섹션 컴팩트화 — `ResultList.tsx:2340`

기존: `p-4 bg-white rounded-2xl` 큰 카드 + `flex-1 py-3` 버튼 2개
변경: 1줄 인라인 `flex items-center justify-center gap-3 py-1.5` + 작은 pill 버튼

```tsx
feedbackSent ? (
  <p className="text-xs text-center py-1.5" style={{ color: 'var(--green-600)' }}>
    감사합니다! 소중한 의견 반영할게요 ✨
  </p>
) : (
  <div className="flex items-center justify-center gap-3 py-1.5">
    <span className="text-xs" style={{ color: 'var(--text-muted)' }}>결과가 도움됐나요?</span>
    <button className="... px-3 py-1 rounded-full text-xs">👍 도움됐어요</button>
    <button className="... px-3 py-1 rounded-full text-xs">👎 별로예요</button>
  </div>
)
```

**효과**: 피드백 섹션 높이 약 80px → 32px로 절반 이상 축소. 결과 더보기/즐겨찾기 CTA 강조 효과.

---

## 다음 구현 예정 (갱신)

1. 🔍 카드 확대 모달 (PlaceDetail 연결, 14점)
2. 🎯 근접도 필터 칩 (proxScore > 70, 9점)
3. 📐 이탈 거리 필터 (+1km/+2km 칩, 8점)
4. 🏷️ 뱃지 1차/2차 그룹핑 — 전체 카드 뱃지 overflow 정리 (7점)
