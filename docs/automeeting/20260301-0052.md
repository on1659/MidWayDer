# AutoMeeting — 2026-03-01 00:52

## 1. 현황 (v0.5.2 기준)

ResultList.tsx (2883줄)에 이미 구현된 주요 기능:
- 필터 칩 9종 (영업중 / 이탈시간 / 거리 / 근접 / 미방문)
- 컴팩트 모드, 핀 고정, 스와이프, 베스트픽 배너, ETA
- 활성 필터 요약 바, Sticky 필터 바, 더보기, 구간별 위치 뱃지
- 검색 타임스탬프, 스코어 분해 토글, 체류시간 설정 등

메모리에 기록된 "다음 구현 예정":
1. 🎯 현재 위치 → 출발지 원탭 (15점)
2. 📍 스크롤 위치 기억 (14점)
3. 📊 필터 효과 미리보기 (13점)  ← 이번 세션 구현
4. 🗺️ 리스트 카드 group by 경로 구간 (12점)  ← 이번 세션 구현
5. 🔔 결과 카드 "이것도 어때요?" (9점)

---

## 2. UX/UI 분석

### 발견된 Pain Points

| 번호 | 문제 | 영향 |
|------|------|------|
| A | 필터 칩을 눌러봐야 결과 수를 알 수 있음 | 시행착오 → 이탈 |
| B | 결과 리스트가 단순 점수순 나열 → "어디서 들를 수 있나?" 파악 어려움 | 탐색 비효율 |
| C | 필터 토글 버튼 줄이 이미 4개 (프리셋2 + 상세필터 + 컴팩트) | 발견성 한계 |
| D | GPS 버튼이 지도에만 있음 (검색 오버레이 내부에는 별도 버튼 존재) | 플로우 단절 |

### 신규 기능 제안 (점수화)

| 기능 | 임팩트 | 난이도 | 점수 |
|------|--------|--------|------|
| 필터 칩 숫자 미리보기 "(N개)" | 9 | 2 | **17** |
| 경로 구간별 그룹핑 토글 | 8 | 4 | **12** |
| GPS → 출발지 원탭 CTA (SearchOverlay 상단) | 7 | 3 | **11** |
| 카드 스태거 진입 애니메이션 | 5 | 2 | **8** |
| 필터 효과 hover 툴팁 | 4 | 4 | **4** (모바일 비적합) |

### 우선순위 결정

**1위: 필터 칩 숫자 미리보기** — 칩에 "(N개)" 표시. 사용자가 필터 적용 전 결과 수 예측 가능.
**2위: 경로 구간별 그룹핑** — "구간" 토글 버튼. 출발직후/초반/중간/후반/도착직전 섹션 헤더 삽입.

---

## 3. 구현 내용

### Feature 1: 필터 칩 숫자 미리보기 (ResultList.tsx)

**추가된 코드:**
```typescript
// 각 필터 칩의 미리보기 카운트 (비활성 상태에서 "(N개)" 표시)
const filterCounts = {
  detour5: results.filter((r) => r.detourCost.duration <= 300).length,
  detour10: results.filter((r) => r.detourCost.duration <= 600).length,
  detour15: results.filter((r) => r.detourCost.duration <= 900).length,
  dist1km: results.filter((r) => r.detourCost.distance <= 1000).length,
  dist2km: results.filter((r) => r.detourCost.distance <= 2000).length,
  proxScore: results.filter((r) => r.proximityScore >= 70).length,
};
```

**UX 변경:** 이탈시간·거리·근접 칩에 `(N개)` 소형 태그 추가. 결과 전체와 같으면 표시 안 함(의미없는 카운트 제거).

### Feature 2: 경로 구간별 그룹핑 (ResultList.tsx)

**추가된 타입:**
```typescript
type RenderItem =
  | { type: 'header'; label: string; count: number }
  | { type: 'card'; result: DetourResult; index: number };
```

**추가된 상태:** `isGrouped: boolean` (기본 false)

**추가된 useMemo:**
```typescript
const renderItems = useMemo((): RenderItem[] => {
  if (!isGrouped) {
    return visibleResults.map((result, index) => ({ type: 'card', result, index }));
  }
  // isGrouped=true: 섹션 헤더 삽입, 페이지네이션 무시 (전체 표시)
  ...
}, [isGrouped, sortedWithPins, visibleResults]);
```

**추가된 헬퍼:**
- `getSegmentEmoji(label)`: 🚦/🛣️/📍/🏁/🎯 이모지 반환

**UI:**
- "🗺️ 구간" 토글 버튼 추가 (간략/자세히 버튼 옆)
- 구간 전환 시 구분선 + "🚦 출발 직후 · 3곳" 형태 섹션 헤더
- isGrouped 시 더보기/접기 버튼 숨김 (전체 표시)
- 새 검색 결과 도착 시 isGrouped 자동 리셋

---

## 4. 변경 파일

- `src/components/search/ResultList.tsx`
  - `getSegmentEmoji()` 헬퍼 추가
  - `RenderItem` 유니온 타입 추가
  - `isGrouped` state 추가
  - `filterCounts` 객체 추가
  - `renderItems` useMemo 추가
  - "🗺️ 구간" 토글 버튼 추가
  - 필터 칩에 `(N개)` 카운트 추가
  - 카드 리스트 렌더링 → `renderItems.map()` 로 변경

---

## 5. 다음 구현 예정 (업데이트)

1. 🎯 현재 위치 → 출발지 원탭 (15점) — SearchOverlay 상단 GPS CTA 버튼
2. 📍 스크롤 위치 기억 (14점) — 지도↔리스트 전환 시 스크롤 위치 유지
3. 🔔 결과 카드 "이것도 어때요?" (9점) — 같은 경로 다른 카테고리 제안
4. 🎨 카드 진입 스태거 애니메이션 (8점) — index × 30ms CSS delay
5. 🔍 GPS 반경 검색 "내 주변만 보기" (8점) — 현재 위치 기준 필터
