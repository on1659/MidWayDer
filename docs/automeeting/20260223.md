# MidWayDer 자동 회의록 (2026-02-23)

## 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (02:50) **E2E 테스트 확장**: Playwright 스모크 테스트가 추가됨. 주요 경로(카테고리 선택, 경유지 선택 후 지도 표시, 결과 상세 토글)까지 커버 범위를 확장하면 회귀 방지 효과가 큼.
- (02:50) **디자인 토큰 전면 적용**: globals.css에 신규 토큰이 들어왔으니, 주요 컴포넌트의 하드코딩 컬러를 토큰으로 통일하면 유지보수 쉬움(다크모드 전환 준비 포함).
- (02:50) **CI 결과 요약 배지/레포트 링크**: GitHub Actions에서 업로드되는 Playwright report를 PR 코멘트로 남기거나, README에 배지 추가.
- (03:50) **CI용 테스트 모드 플래그**: Kakao SDK/지도 의존성이 CI에서 깨질 수 있으니, `NEXT_PUBLIC_TEST_MODE` 같은 플래그로 지도 로딩/외부 스크립트 최소화 옵션 고려.
- (04:50) **UI 토큰 적용 범위 점검**: detour badge/경고 톤 등 일부가 아직 하드코딩 색상. `--warning`, `--accent-weak` 등으로 통일하면 테마 일관성 좋아짐.
- (04:50) **Playwright 리포트 링크 노출**: GitHub Actions 업로드 아티팩트의 URL을 PR 코멘트로 남기도록 action 추가하면 QA 효율 상승.
- (05:50) **스플래시 대기 안정화**: E2E에서 `MidWayDer` 텍스트 숨김만 체크하는 대신, 스플래시 오버레이에 `data-testid`를 달아 사라짐을 명확히 기다리면 flake 감소.
- (05:50) **지도 로드 에러 최소화**: CI/Test 모드에서 Kakao SDK 로드 실패 메시지를 숨기거나 mock 처리하면 실패 스크린샷 노이즈 줄어듦.
- (06:50) **디자인 토큰 미적용 영역 정리**: SearchOverlay 헤더 back 아이콘 색상/AddressInput focus 링 등 아직 하드코딩 일부 남음 → 전면 토큰화하면 유지보수 쉬움.
- (06:50) **E2E 케이스 추가**: `mobile-origin-input`, `mobile-destination-input`, `mobile-search-route-btn` testId 추가됨 → 입력/버튼 disabled/로딩 상태까지 smoke에 포함하면 안정성 상승.
- (07:50) **스플래시 대기 안정화 테스트 패턴**: `MidWayDer` 텍스트 기반 wait 대신 스플래시 오버레이 testId 기준으로 대기하면 데스크톱 헤더 중복 텍스트와 충돌 방지.

## 2) 당장 수정해야 할 사항 (우선순위)
- (02:50) **테스트 산출물 제외 처리**: `playwright-report/`가 생성되며 git status에 노출됨. `.gitignore`에 `playwright-report/`, `test-results/` 추가 필요.
- (02:50) **E2E 관련 파일 일괄 커밋 정리**: `.github/workflows/ci.yml`, `playwright.config.ts`, `tests/` 폴더가 untracked 상태. CI/테스트 도입이 목적이면 한 번에 커밋 정리 필요.
- (03:50) **미커밋 변경 정리 필요**: `package.json/lock`, UI 토큰/간격 변경, 테스트 관련 파일이 모두 수정/추가 상태. 의도된 변경이면 커밋, 아니면 롤백/정리 필요.
- (04:50) **CI에서 지도 의존성 테스트 실패 가능성 점검**: Playwright smoke가 지도 로딩/외부 SDK에 의존하는지 확인 필요. 깨진다면 test-mode 플래그/route mock 확대 적용.
- (05:50) **스플래시 식별자 추가 필요**: 현재 스플래시 제거 확인이 텍스트 기반이라 흔들림 가능. 스플래시에 `data-testid` 추가 후 E2E에서 사용하면 안정화.
- (06:50) **E2E 인프라 미커밋 정리 필요**: `.github/`, `tests/`, `playwright.config.ts`, `package.json` 변경이 통째로 untracked/modified 상태 → 목적 확정 후 한 번에 커밋 권장.
- (06:50) **Playwright 브라우저 설치 단계 확인**: CI에서 `npx playwright install --with-deps` 누락 시 실패 가능 → 워크플로우에 명시 필요.
- (06:50) **입력 UI 패딩/폰트 변경 QA**: AddressInput/Overlay spacing·font-size 조정됨 → 모바일/데스크톱 레이아웃 깨짐 여부 확인 필요.
- (07:50) **E2E 스플래시 대기 실패 수정(완료)**: splash overlay에 `data-testid` 추가 + `waitAppReady`가 해당 요소 기준으로 대기하도록 수정.

## 3) 최근 변경 요약
- (02:50) **UI 토큰 도입**: CSS 변수 체계 추가 및 일부 컴포넌트 색상/간격 조정.
- (02:50) **테스트 인프라**: Playwright 도입 + CI 워크플로우 추가 + data-testid 속성 보강.
- (03:50) **gitignore 보강**: Playwright 산출물(`playwright-report/`, `test-results/`) 제외 처리 완료.
- (04:50) **모바일 입력/버튼 testId 추가**: 오버레이 입력/검색 버튼에 testId 부여로 E2E 안정성 개선.
- (04:50) **버튼/텍스트 컬러 토큰화 진행**: PlaceDetail/ResultList 등 일부 컴포넌트에 토큰 적용.
- (05:50) **E2E 안정화 제안 기록**: 스플래시 오버레이 식별자/지도 로드 에러 처리 등 테스트 안정화 포인트 추가.
- (06:50) **UI 토큰/간격 조정 + testId 추가**: AddressInput/SearchOverlay/ResultList/BottomSheet 등에서 컬러 토큰 적용, 입력/버튼 testId 부여, 간격/폰트 사이즈 조정.
- (07:50) **스플래시 testId 도입**: 스플래시 오버레이에 testId 추가 후 E2E 대기 로직을 해당 요소 기준으로 변경.

## 4) 08:50 회의 메모
### 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (08:50) **다크모드 토글 UI**: theme.css + 초기 theme-init 스크립트가 들어왔으니, 헤더/설정에 토글 추가 + 시스템 테마 연동(`prefers-color-scheme`) 권장.
- (08:50) **테마 상태 동기화**: `localStorage`만 보는 방식이라 최초 로드 시 시스템 테마를 반영하는 옵션 추가하면 UX 개선.
- (08:50) **color-scheme 메타 추가**: `<meta name="color-scheme" content="light dark">`로 폼/스크롤바 기본 색상을 OS에 맞추면 일관성 상승.
- (08:50) **토큰 적용 범위 점검**: 새 theme.css 도입으로 하드코딩 컬러가 남은 컴포넌트 우선 정리(알림 배지/링/아이콘 등).

### 2) 당장 수정해야 할 사항 (우선순위)
- (08:50) **미커밋 변경 정리**: `src/app/theme.css` 추가 + `globals.css`, `layout.tsx` 수정이 남아 있음 → 의도된 변경이면 커밋, 아니면 롤백.
- (08:50) **다크모드 토글 부재**: 스크립트로 클래스만 붙이고 UI/설정이 없음. 사용자 전환 경로 없으면 반쪽 기능이니 추가 여부 판단 필요.

### 3) 최근 변경 요약
- (08:50) **테마 토큰 분리**: CSS 변수들을 `theme.css`로 분리, `globals.css`는 기본 레이아웃/간격 중심으로 정리.
- (08:50) **초기 다크모드 적용 스크립트**: `layout.tsx`에서 `localStorage` 테마를 읽어 `theme-dark` 클래스 부여.

## 5) 09:50 회의 메모
### 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (09:50) **모바일 다크모드 토글**: 현재 데스크톱 헤더에만 토글이 있어 모바일 진입 경로 없음 → SearchOverlay 상단/설정 아이콘으로 추가 권장.
- (09:50) **시스템 테마 자동 동기화**: `prefers-color-scheme` 감지 + 변경 이벤트 대응하면 OS 테마 변경 시 UX 개선.
- (09:50) **theme-color 동적 갱신**: 다크모드일 때 주소창/상태바 색상이 고정됨 → 테마 토글 시 meta `theme-color`를 업데이트.
- (09:50) **다크 토큰 적용 영역 확장**: Map/BottomSheet 내부의 하드코딩 색상(아이콘, 보더, hover) 추가 토큰화.

### 2) 당장 수정해야 할 사항 (우선순위)
- (09:50) **meta theme-color에 CSS 변수 사용**: `content="var(--accent)"`는 브라우저가 해석하지 않을 수 있음 → 초기값은 hex로 두고, 토글 시 JS로 갱신 필요.
- (09:50) **테마 초기화 중복 정리 필요**: layout의 `theme-init` + page의 useEffect 둘 다 존재 → 역할 분리(초기 DOM 적용/React state 동기화) 명확히 하거나 하나로 통합 검토.

### 3) 최근 변경 요약
- (09:50) **다크모드 토글 UI 추가(데스크톱)**: 헤더에 토글 버튼 추가, `theme-dark` 클래스/로컬스토리지 연동.
- (09:50) **컬러 토큰 적용 확대**: 스플래시/최근검색/버튼 등 하드코딩 컬러를 theme 토큰으로 대체.

## 6) 10:50 회의 메모
### 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (10:50) **모바일 다크모드 토글 위치 개선**: 상단 버튼은 지도 위에 떠서 접근성이 떨어질 수 있음 → SearchOverlay 안에 토글 넣으면 자연스러움.
- (10:50) **theme-color/주소창 색상 다크 반영**: 테마 변경 시 메타 색상 갱신을 자동화해 PWA/모바일 주소창 컬러 반영.

### 2) 당장 수정해야 할 사항 (우선순위)
- (10:50) **theme-color 메타 값 수정 필요**: `var(--accent)`는 브라우저가 해석 못할 수 있음 → hex 기본값 + JS 동기화로 변경 필요.

### 3) 최근 변경 요약
- (10:50) **theme-color 메타 동기화 작업 진행**: theme 토글 시 `meta[name="theme-color"]`를 현재 `--accent` 값으로 갱신하는 방향으로 정리.

## 7) 11:50 회의 메모
### 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (11:50) **다크모드 시스템 동기화 + 변경 감지**: `prefers-color-scheme` 감지 + 이벤트 리스너로 OS 테마 변경 시 자동 반영.
- (11:50) **다크모드 토글 UX 보강**: 모바일에서 접근 경로 확보(Overlay 상단/설정 영역) + 현재 상태가 보이는 토글 디자인.
- (11:50) **컬러 토큰 점검 체크리스트**: Map/Marker/BottomSheet/ResultList 등에서 하드코딩 컬러 남은 부분을 정리하는 체크리스트 문서화.

### 2) 당장 수정해야 할 사항 (우선순위)
- (11:50) **theme.css 미커밋 상태**: `src/app/theme.css`가 untracked인데 `globals.css`에서 import 중 → 현재 브랜치 기준으로 CI/배포에 누락 위험. 의도된 변경이면 관련 수정사항 묶어서 커밋 필요.
- (11:50) **테마 초기화 중복 확인**: `layout`의 theme-init + `page.tsx` 테마 초기화가 중복됨 → 역할 분리(초기 DOM 적용/React state) 또는 하나로 정리 필요.
- (11:50) **대량 수정분 커밋 정리 필요**: map/search 컴포넌트에 토큰 적용 변경이 넓게 퍼져 있음 → 의도된 변경이면 커밋 분리(테마/토큰 vs 기능 변경) 권장.

### 3) 최근 변경 요약
- (11:50) **테마 토큰 적용 확장 진행 중**: map/marker/search UI에 토큰화 변경(컬러/보더/배경) 반영.
- (11:50) **theme-color 기본값 유지 + JS 동기화**: meta 기본값은 hex로 유지하고, 클라이언트에서 `--accent`로 갱신하도록 구현됨.

## 8) 12:50 회의 메모
### 1) 최근 변경/이슈 기반 추가하면 좋을 기능
- (12:50) **다크모드 자동 동기화 완성**: `prefers-color-scheme` 변경 이벤트까지 연결해 OS 테마 변경을 즉시 반영.
- (12:50) **모바일 토글 접근성 개선**: SearchOverlay 상단/설정 영역에 테마 토글 고정 노출.
- (12:50) **테마 토큰 점검 리스트 문서화**: Map/Marker/BottomSheet/ResultList 등 남은 하드코딩 컬러 체크리스트 추가.

### 2) 당장 수정해야 할 사항 (우선순위)
- (12:50) **theme.css 커밋 누락 해결 필요**: `globals.css`에서 import 중이라 배포 시 누락 위험 → `theme.css` 포함한 토큰 변경분 커밋 필요.
- (12:50) **테마 초기화 중복 정리**: `layout`의 theme-init + `page.tsx` 초기화가 중복 → 역할 분리 또는 통합 정리 필요.

### 3) 최근 변경 요약
- (12:50) **토큰화 적용 확대**: Map/Marker/검색 UI의 하드코딩 컬러를 테마 토큰으로 치환.
- (12:50) **테마 토큰 분리 유지**: `theme.css`로 변수 분리 유지, `globals.css`는 레이아웃/공통 스타일 중심 정리.
