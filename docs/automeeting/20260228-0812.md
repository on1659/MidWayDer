# MidWayDer Auto-Meeting — 2026-02-28 08:12

## 현황 (v0.5.1 기준)

- 기능 포화 상태: ResultList에 스와이프·핀·필터·ETA·방문·인기도 등 총 20+ 기능 탑재
- 지도 영역 재검색·미방문 필터·MultiStop 등 MEMORY 상 "다음 구현 예정" 3건 모두 이미 구현 완료 (메모리 업데이트 필요)
- TypeScript 컴파일 에러 없음

---

## UX/UI 분석

### 발견한 버그

| # | 위치 | 증상 | 심각도 |
|---|------|------|--------|
| B1 | ResultList.tsx:1110 | `unvisitedOnly` 필터 적용 → 모든 결과 미노출 시 빈 화면 (피드백 없음) | 🔴 High |
| B2 | ResultList.tsx:1116 | 필터 초기화 버튼에 `setUnvisitedOnly(false)` 누락 → 미방문 필터 해제 불가 | 🟠 Medium |

### 기능 제안 (점수화)

| 순위 | 기능 | 설명 | 점수 | 구현비용 |
|------|------|------|------|---------|
| 1 | 🚀 **베스트 픽으로 바로 출발** | 결과 헤더에 원탭 버튼 → 최상위 결과로 즉시 네비 실행 | 72 | Low |
| 2 | 📌 **카드 핀 고정** | 특정 카드를 항상 최상단에 고정 (정렬 무관) | 68 | Low |
| 3 | 🎯 **현재 위치 기준 가장 가까운 강조** | GPS 위치와 비교해 "지금 내 위치 기준 N분" 표시 | 70 | Medium |
| 4 | 💬 **경유지 메모 추가** | 장소 카드에 개인 메모 입력 (localStorage) | 65 | Medium |
| 5 | 📊 **경로 분포 미니 차트** | 헤더에 "출발 직후 N·중반 N·도착 직전 N" 시각화 | 62 | Low |
| 6 | 🗺️ **다중 경유지 최적 루트 공유** | MultiStop 결과를 URL로 공유 | 58 | High |

---

## 우선순위 (이번 세션)

1. **[즉시]** 버그 B1, B2 수정
2. **[즉시]** 🚀 베스트 픽으로 바로 출발 버튼 (72점) 구현
3. **[즉시]** 📌 카드 핀 고정 (68점) 구현

---

## 구현 내용

### 수정 파일
- `src/components/search/ResultList.tsx`

### Bug Fix B1 — unvisitedOnly 빈 상태 메시지
```diff
- {filteredResults.length === 0 && (openNowOnly || maxDetourMin !== null || nameFilter.trim()) && (
+ {filteredResults.length === 0 && (openNowOnly || maxDetourMin !== null || unvisitedOnly || nameFilter.trim()) && (
+   // 메시지도 unvisitedOnly 경우 분기:
+   unvisitedOnly ? '이미 방문한 곳이에요. 필터를 해제해보세요' : ...
```

### Bug Fix B2 — 필터 리셋 버튼에 setUnvisitedOnly 추가
```diff
- onClick={() => { setOpenNowOnly(false); setMaxDetourMin(null); setNameFilter(''); }}
+ onClick={() => { setOpenNowOnly(false); setMaxDetourMin(null); setUnvisitedOnly(false); setNameFilter(''); }}
```

### 신규 기능 1 — 🚀 베스트 픽으로 바로 출발 (72점)

- **위치**: 결과 요약 헤더 → 출발 시각 입력 아래 풀 폭 버튼
- **동작**:
  1. 출발 시각을 현재 시각으로 자동 설정
  2. 핀 고정 카드가 있으면 그것을, 없으면 sortedWithPins[0] (최상위 결과)를 즉시 네비 실행
- **UI**: 파란 그라디언트 + 🚀 아이콘, 핀 기준일 때 "(📌 고정 기준)" 표시
- **구현**: `handleQuickGo` + `triggerNav` 헬퍼 추출 (handleOpenNavi 재사용)

### 신규 기능 2 — 📌 카드 핀 고정 (68점)

- **상태**: `pinnedIds: Set<string>` (새 검색 결과 시 자동 초기화)
- **정렬**: `sortedWithPins = [...pinned, ...rest]` — filteredResults에서 핀 먼저
- **UI**:
  - 컴팩트 모드: 행 우측에 Bookmark 아이콘 버튼
  - 전체 카드 모드: 우측 액션 컬럼 하단에 Bookmark 버튼
  - 핀 고정 시: 카드 우상단 "📌 상단 고정" accent 뱃지
- **QuickGo 연동**: 핀 고정 시 해당 카드로 바로 출발

---

## 코드 변경 요약

| 구분 | 변경 내용 |
|------|-----------|
| import | `Bookmark` 추가 (lucide-react) |
| state | `pinnedIds: Set<string>` 추가 |
| memo | `sortedWithPins` 추가, `visibleResults` 소스를 `sortedWithPins`로 변경 |
| handler | `triggerNav`, `handleTogglePin`, `handleQuickGo` 추가 |
| header | 🚀 바로 출발 버튼 + 빠른 출발 기능 |
| card | 컴팩트/전체 모드 Bookmark 버튼 + 핀 뱃지 |
| bug fix | unvisitedOnly 빈 상태 + 필터 리셋 수정 |
| 더보기 | `filteredResults` → `sortedWithPins` 참조 수정 |

---

## 다음 구현 예정

1. 🎯 현재 위치 기준 가장 가까운 장소 강조 (70점)
2. 💬 경유지 개인 메모 추가 (65점) — localStorage 기반
3. 📊 경로 분포 미니 차트 (62점) — 헤더에 출발직후/중반/도착직전 분포

---

## 메모리 업데이트 필요 사항

- ✅ 지도 영역 재검색 이미 구현 완료 (page.tsx:862-887)
- ✅ 미방문만 보기 이미 구현 완료 (ResultList.tsx `unvisitedOnly`)
- ✅ MultiStopSelector 이미 구현 완료 (page.tsx:792-808)
